using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor;
using System.Linq;

public class MaterialDebug
{
    /*
    [MenuItem("Dev/MaterialDebug")]
    private static void Method()
    {
        if (Selection.activeObject is GameObject gameObject && gameObject.TryGetComponent(out MeshRenderer renderer))
        {
            Debug.Log(string.Join(System.Environment.NewLine, renderer.sharedMaterial.enabledKeywords.Select(keyword => keyword.name)));
        }
    }
    */
}
